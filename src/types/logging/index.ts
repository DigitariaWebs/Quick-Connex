/**
 * Logging Types
 * 
 * Logging system types.
 */

// Re-export from logging service types for now
export type {
  LogLevel,
  LogCategory,
  LogContext,
  LogEntry,
  LogConfig
} from '@/lib/logging/core/types';

